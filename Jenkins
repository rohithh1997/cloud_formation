pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name calculator1 --template-body file://jenkins_cal.yaml --region 'us-east-1'"
              }
             }
            }
            }
