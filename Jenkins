pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation update-stack --stack-name calculator --template-body file://s3.yaml --region 'us-east-1'"
              }
             }
            }
            }
